package com.nmanojlovic.smartcinema.constants;

public enum DatabaseConstants {
    FROM,
    WHERE,
    AND,

    /** TABLES **/
    USER,
    PRIVILEGE,
    ROLE,

    /** FIELDS **/
    EMAIL,
    PASSWORD
}
